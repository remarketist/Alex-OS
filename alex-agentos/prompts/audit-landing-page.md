# Prompt: Audit a landing page

---
Run the `landing-page-audit` skill on this URL: <URL>

Fetch the page. Score all 9 dimensions (1-5) as a cold visitor who just clicked an ad on mobile. Use the `landing-page-cro-agent`. Output using `templates/landing-page-audit-template.md`: score table, conversion diagnosis (the single biggest killer), priority fixes each with the business reason and rough impact, a rewritten above-the-fold, a revised section-by-section structure, and tracking gaps to fix before spending on traffic.

Context (if available): the offer is <OFFER>, traffic comes from <Meta/Google> with this ad promise: <AD ANGLE>. Judge message-match against it.
